{
  "default_max_stamina": {
    "description": "全局默认体力上限",
    "type": "int",
    "hint": "当用户尚未通过指令设置个人上限时，插件计算所采用的初始值（通常为25或50）。",
    "default": 25,
    "options": [
      25,
      50
    ]
  },
  "recovery_interval_minutes": {
    "description": "体力恢复速率（分钟/点）",
    "type": "int",
    "hint": "恢复1点体力所需的分钟数。根据描述，默认固定为30分钟。",
    "default": 30,
    "obvious_hint": true
  },
  "task_persistence": {
    "description": "任务持久化",
    "type": "bool",
    "hint": "开启后，若机器人重启，插件会尝试从数据库恢复未完成的任务。关闭则重启后任务清空。",
    "default": true
  },
  "admin_only_settings": {
    "description": "仅限管理员修改上限",
    "type": "bool",
    "hint": "若开启，普通用户将无法通过指令修改自己的体力上限，通常保持关闭。",
    "default": false
  },
  "log_level": {
    "description": "日志详细程度",
    "type": "string",
    "hint": "控制插件在后台输出日志的详细程度。",
    "default": "INFO",
    "options": [
      "DEBUG",
      "INFO",
      "WARNING",
      "ERROR"
    ]
  }
}
